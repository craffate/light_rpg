@ECHO OFF

SET NAME=lightrpg.smx
SET ENV_PATH=env
SET ENV_INCLUDES_PATH=%ENV_PATH%\include
SET SOURCE_PATH=srcs
SET INCLUDES_PATH=includes
SET BUILD_PATH=build
SET SOURCE_FILE=lightrpg.sp
SET CC=%ENV_PATH%\spcomp.exe

IF NOT EXIST %BUILD_PATH% (MKDIR %BUILD_PATH%)

%CC% -D%~dp0 -E -t8 -z9 -i%ENV_INCLUDES_PATH% -i%INCLUDES_PATH% -o%BUILD_PATH%\%NAME% %SOURCE_PATH%\%SOURCE_FILE%

PAUSE

COMPILE.BAT
